@model Utente

<!-- partial:index.partial.html -->
<div class="pt-5">
  <h1 class="text-center">Login</h1>
  
<div class="container">
                <div class="row">
                    <div class="col-md-5 mx-auto">
                        <div class="card card-body mb-5">
                                                    
                            <form id="submitForm" action=@Url.Action("login", "Utente") method="post" data-parsley-validate="" data-parsley-errors-messages-disabled="true" novalidate="" _lpchecked="1"><input type="hidden" name="_csrf" value="7635eb83-1f95-4b32-8788-abec2724a9a4">
                            @Html.AntiForgeryToken()
                                <div class="form-group required">
                                    <lSabel class="d-flex w-100 flex-row align-items-center" for="username">Username / Email</lSabel>
                                    <input asp-for="nomeUtente" type="text" class="form-control">
                                </div>                    
                                <div class="form-group required">
                                    <label class="d-flex w-100 flex-row align-items-center" for="password">Password </label>
                                    <input asp-for="password" type="password" class="form-control">
                                </div>
                                <div class="form-group pt-1">
                                    <button class="btn btn-warning btn-block" type="submit">Log In</button>
                                </div>
                            </form>
                            <p class="small-xl pt-3 text-center">
                                <span class="text-muted">Not a member?</span>
                                <a href="https://localhost:7137/utente/registrazione">Sign up</a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
</div>
<!-- partial -->